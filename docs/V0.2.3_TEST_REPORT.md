# v0.2.3 offline verification report

Verified on 2026-07-28 in an isolated `/tmp` Git worktree. No test used the
production state directory, sent Telegram messages, or submitted ComfyUI jobs.

- All shell scripts: `bash -n` passed.
- Python implementation and tests: `compileall` passed.
- Every JSON file and the state schema parsed successfully.
- Workflow raw SHA-256:
  `6e5c6b879168e31ceb9af04fd6ffa4d4412618398699e415681b7d45a43a5f3d`.
- Workflow masked SHA-256 and default baseline:
  `279d35360af5a1d5ed402cf11855732237b199130aeba1741d5483ce6db17208`.
- Existing unittest suite: 8 tests run, 8 passed.
- CLI help retained `context`, `status`, `history`, `update`, `show`, `render`,
  `resend-last`, `doctor`, `jobs`, `job-status`, `wait`, `backup`, and `_worker`.
- Sensitive-value scan found no token/API-key/password pattern.
- The restoration script passed static shell parsing and was not executed.

The ComfyUI `/system_stats` endpoint was checked read-only before the snapshot
and reported ComfyUI 0.28.0. No online generation or delivery test was run.
