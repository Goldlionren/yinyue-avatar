# v0.2.4 test report

Status: offline development verification passed on 2026-07-28.

The suite covers T1 cross-scene cleanup, T2 continue, T3 replace, T4 layer, T5
shower damp state, T6 reset relationship/memory protection, T7 mocked ComfyUI
failure rollback, T8 lossless/idempotent v1 migration, strict-field rejection,
migration backup/event behavior, and all eight v0.2.3 tests.

No test targets production state, sends Telegram, or submits a real ComfyUI
prompt.

Results:

- Shell syntax, Python compile, JSON parse, schema validation, and `git diff
  --check`: passed.
- Workflow raw and masked hashes: unchanged from v0.2.3 and passed.
- Unit/mock suite after offline completion work: 55 tests run, 55 passed.
- Original v0.2.3 suite: 8 tests run, 8 passed without skips or relaxation.
- New transition/migration/transaction suite: 10 tests run, 10 passed.
- Author relationship suite: 17 tests covering CLI and function-level
  file/inline complete replacement,
  non-merge deletion, schema rejection and rollback, break-glass confirmation,
  future fields, non-object rejection, private backup creation, restore,
  non-relationship canonical preservation, last-image preservation, zero
  jobs/ComfyUI/Hermes calls, audit contents, partial relationship update, and
  legacy compatibility patching.
- Added relationship/worker lock serialization, restore path traversal,
  out-of-root/directory/symlink rejection, and backup retention tests.
- Added structured memory CRUD, dedupe, supersede, resolve, schema/length
  validation, legacy bootstrap, deterministic summary sorting/budget,
  rollback-on-failure, backup retention, context isolation, and `--remember`
  compatibility tests.
- Added meaningful-history allowlist, event/fingerprint dedupe, priority,
  count/character budget, chronological output, corrupt-line tolerance, and
  relationship compaction tests.
- Safe-script tests passed for installer dry-run, dangerous target rejection,
  local config preservation, injected post-switch rollback, staging namespace,
  Telegram/worker disablement, marker validation, symlink rejection, and
  cleanup.
- All old CLI names plus transition/reset commands are present.
- A temporary copy of the actual production revision-196 state migrated
  losslessly, retained revision and `last_image`, and was idempotent on a second
  run.
- Sensitive-value scan: passed.

Not executed: real ComfyUI generation, Telegram text/media delivery, production
state migration, production deployment, and restoration with `--apply`.
