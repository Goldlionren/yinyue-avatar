# v0.2.4 Real ComfyUI Staging Test Report

Test date: 2026-07-28 (Australia/Sydney)

## Conclusion

The isolated real-ComfyUI staging acceptance passed T1 through T4. The three
successful foreground renders completed in 127.24, 130.76, and 132.64 seconds.
The forced connection failure left the complete candidate state unchanged and
created only a failed job record.

No production Skill or production state file changed. No Telegram delivery was
attempted, no detached or systemd worker was started, and no production
migration or deployment was performed.

An initial T1 invocation lost its local command-runner handle after submission.
The ComfyUI prompt subsequently completed in about 126.9 seconds and the
foreground process completed its staging commit. T1 was repeated under a
persistent PTY and completed normally. Both generated artifacts and the
orphan-run evidence are retained. This was a test harness process-supervision
observation, not a production write.

## Git and staging baseline

- Branch: `feature/v0.2.4-long-term-memory-scene-state`
- Commit: `e9769c9fb8787954f88f390a0879003a1d3c317e`
- Required ancestors: `94feb48` and
  `a22b703eded1d4be7118a5b4036879a2a6fa825e`
- Checkout was clean before staging preparation.
- Successful staging root:
  `/mnt/data_nvme/tmp/yinyue-avatar-staging-20260728T064000Z`
- Failure-test staging root:
  `/mnt/data_nvme/tmp/yinyue-avatar-staging-20260728T065000Z-failure`
- Both roots are regular directories in the permitted
  `/mnt/data_nvme/tmp/yinyue-avatar-staging-*` namespace.
- Both marker files exist as regular mode `0600` files.
- No staging-tree symbolic links were present.
- The successful staging tree is retained for evidence; cleanup was not run.

`scripts/prepare-staging.sh` was run in dry-run mode before `--apply`. The
prepared Skill came from the feature checkout. State was copied from production,
and the workflow was copied again from the authoritative production Skill.
Skill, state, jobs, pending, logs, and output paths are isolated under the
staging root.

## ComfyUI and timeout configuration

- Base URL: `http://192.168.100.211:8188`
- Read-only `/system_stats` result: ComfyUI `0.28.0`, Python `3.13.11`,
  PyTorch `2.11.0+cu130`, NVIDIA GeForce RTX 3060.
- Connection/request timeout: 15 seconds.
- Generation timeout: 300 seconds.
- Poll interval: 2 seconds.
- Foreground command outer timeout: `timeout --signal=TERM --kill-after=30s 420s`.

The current implementation uses `comfyui.connect_timeout_seconds`,
`comfyui.generation_timeout_seconds`, and
`comfyui.poll_interval_seconds`. Only these existing keys were overridden in
the staging-local configuration.

## Isolation and delivery controls

The staging-local configuration contained:

```json
{
  "runtime": {
    "state_dir": "/mnt/data_nvme/tmp/yinyue-avatar-staging-20260728T064000Z/state",
    "launcher": "disabled"
  },
  "telegram": {
    "enabled": false,
    "default_channel": "DISABLED",
    "hermes_cli": "/mnt/data_nvme/tmp/yinyue-avatar-staging-20260728T064000Z/skill/bin/hermes-send-disabled"
  }
}
```

Every successful result reported
`delivery={"ok":true,"attempted":false,"skipped":true}`. Process inspection
after the tests found no staging `_worker` or `avatarctl` process. The launcher
was never changed from `disabled`.

The test used only the allowed ComfyUI endpoints: `/system_stats`, `/prompt`,
`/history`, and `/view`.

## T1: v0.2.3-compatible foreground show

Command:

```bash
timeout --signal=TERM --kill-after=30s 420s \
  bin/avatarctl show --foreground --no-send
```

Controlled retry result:

- Start: `2026-07-28T16:36:54+10:00`
- End: `2026-07-28T16:39:01+10:00`
- Elapsed: 127.24 seconds
- Job: `c1d82e25596d4ee2b37ec0449d4297df`
- Prompt: `6a04419d-c63f-4d87-8c5e-cffd6611eb62`
- Status: `completed`
- Revision: 197 to 198
- Image:
  `/mnt/data_nvme/tmp/yinyue-avatar-staging-20260728T064000Z/state/output/yinyue-20260728-163901-r0198.png`
- Image metadata: PNG, 1632 by 2176, RGB, 3.71219 MiB.
- History contains the successful `show` event.
- No delivery attempt was made.

The copied v0.2.3 state began at revision 196. The first invocation performed
the one-time schema migration, then generated revision 197 before the controlled
retry generated revision 198. Its prompt was
`bfb98f30-7fb0-4420-899b-ae1c23fe8baa`; retained ComfyUI history shows
`execution_success` after approximately 126.9 seconds. This explains the two
revision increases from the original copied state and is recorded rather than
hidden.

## T2: complete new-scene break

The staging-only fixture was set to a gym, sportswear, heavy sweat,
post-workout dirt, bent-over panting, a held towel, gym props, and a gym-camera
description. Relationship and memory hashes were captured before rendering.

Command:

```bash
timeout --signal=TERM --kill-after=30s 420s \
  bin/avatarctl transition \
  --json '{"transition_type":"new_scene","actions":[{"type":"shower"},{"type":"replace_outfit","preset":"干净休闲服"},{"type":"move","location":"图书馆"},{"type":"start_activity","activity":"在图书馆休闲看书"}]}' \
  --render --foreground --no-send
```

Result:

- Start: `2026-07-28T16:40:08+10:00`
- End: `2026-07-28T16:42:19+10:00`
- Elapsed: 130.76 seconds
- Job: `af86c8337c494c148e3d5143b77409d5`
- Prompt: `ffd9df11-f2d0-4b85-b5e8-f7850a75aeee`
- Status: `completed`
- Revision: 198 to 199
- Image:
  `/mnt/data_nvme/tmp/yinyue-avatar-staging-20260728T064000Z/state/output/yinyue-20260728-164219-r0199.png`
- Image metadata: PNG, 1632 by 2176, RGB, 3.84902 MiB.

Final state:

- Location and environment are `图书馆`.
- Outfit base is only `干净休闲服`.
- Cleanliness is `clean`; sweat and dirt are `none`; wetness is `dry`.
- Pose, expression, gaze, held objects, props, lighting, and camera were cleared.
- The action is `在图书馆休闲看书`.
- The old gym, sportswear, heavy sweat, post-workout dirt, panting, towel, and
  gym-camera strings have zero matches in final `state.json`.
- Relationship canonical SHA-256 remained
  `d100a54d9421a2035e8b008206e2a086f2982bede911dd2ac402a0df5a30abe5`.
- State memory canonical SHA-256 remained
  `ef272b9ac599dff324947fb1050af08d2ae81508c7da899842e19fbba0a5d3b9`.
- Structured memory file manifest was unchanged.

Manual image inspection:

- No sportswear, visible sweat, wet post-shower appearance, panting pose, towel,
  or gym environment remains.
- The setting is clearly a library and the clothing is clean light casual wear.
- The subject is actively holding/reading a book.
- A close POV/selfie-like framing remains visible. It contains no gym content,
  but is noted as a composition characteristic of the authoritative production
  workflow rather than silently classified as fully neutral framing.

The complete canonical state diff is retained as `T2-state.diff` in the
successful staging root.

## T3: continue in the same scene

Command:

```bash
timeout --signal=TERM --kill-after=30s 420s \
  bin/avatarctl transition \
  --json '{"transition_type":"continue","actions":[{"type":"set_pose","pose":"坐在图书馆座位上看书"},{"type":"set_action","action":"继续看书，把书翻到下一页"}]}' \
  --render --foreground --no-send
```

Result:

- Start: `2026-07-28T16:43:49+10:00`
- End: `2026-07-28T16:46:01+10:00`
- Elapsed: 132.64 seconds
- Job: `41f8f73398c844ecb444d4e44c7e4926`
- Prompt: `cb5e0b1c-ee8f-4588-b537-5f2b8386d705`
- Status: `completed`
- Revision: 199 to 200
- Image:
  `/mnt/data_nvme/tmp/yinyue-avatar-staging-20260728T064000Z/state/output/yinyue-20260728-164601-r0200.png`
- Image metadata: PNG, 1632 by 2176, RGB, 3.87157 MiB.

The scene ID stayed exactly `scene-r0199`. The library, clean casual outfit,
clean/dry physical state, and activity were retained. Only the requested pose
and action changed; shower was not re-applied. Relationship and memory hashes
remained identical to T2. Manual inspection shows the same library and clothing,
with the subject seated and a book visibly open at a page-turn interaction.

The canonical state diff is retained as `T3-state.diff`.

## T4: failed-render transaction rollback

An independent copy was created at
`/mnt/data_nvme/tmp/yinyue-avatar-staging-20260728T065000Z-failure`. Its Skill,
state, output, blocker, marker, and configuration paths point inside that root.
It has no symbolic links. Its ComfyUI URL was set only in its local config to
the unreachable documentation address `http://192.0.2.1:9`, with a five-second
connection timeout and 25-second generation timeout.

Command:

```bash
timeout --signal=TERM --kill-after=5s 30s \
  bin/avatarctl transition \
  --json '{"transition_type":"continue","actions":[{"type":"set_expression","expression":"专注阅读"}]}' \
  --render --foreground --no-send
```

Result:

- Start: `2026-07-28T16:51:29+10:00`
- End: `2026-07-28T16:51:34+10:00`
- Elapsed: 5.15 seconds
- Exit code: 2
- Failed job: `25279a4e86594b4f9485704e4b4be986`
- Job status: `failed`
- Error: `HTTP 请求失败：<urlopen error timed out>`
- Prompt ID: none; the unreachable endpoint accepted no prompt.

Rollback evidence:

- Complete canonical state hash before and after:
  `f8213a05f6ce876c8359140d9f05c5c9c2a5da28e5fa2e83008a9f8b91dd0d2a`.
- Relationship hash before and after:
  `d100a54d9421a2035e8b008206e2a086f2982bede911dd2ac402a0df5a30abe5`.
- Memory hash before and after:
  `ef272b9ac599dff324947fb1050af08d2ae81508c7da899842e19fbba0a5d3b9`.
- Revision remained 200.
- `last_image` remained the copied revision-200 image path.
- History remained exactly 201 lines; no success event was written.
- Job count increased from 204 to 205 only because the failed job record was
  retained.
- Telegram remained disabled and no delivery was attempted.

## Production immutability

Before and after the tests, SHA-256 manifests were independently generated for
every regular file under:

- `/home/james/.hermes/skills/roleplay/yinyue-avatar`
- `/home/james/.hermes/state/yinyue-avatar`

Both complete before/after manifests compare byte-for-byte identical.

Production summary before and after:

| Item | Value |
| --- | --- |
| Revision | 196 |
| Last image | `/home/james/.hermes/state/yinyue-avatar/output/yinyue-20260728-142449-r0196.png` |
| Jobs | 200 |
| Output files | 196 |
| `state.json` SHA-256 | `182261244b90621ce7989db5aa5c0f2ba6124d5ec550a3f6334d7340173e443d` |
| `history.jsonl` SHA-256 | `85b950acb159d31255b19d09c1f3529870dbea2f6377af8e409e62ed4fe812f0` |
| Workflow raw SHA-256 | `6e5c6b879168e31ceb9af04fd6ffa4d4412618398699e415681b7d45a43a5f3d` |

Therefore the production Skill, workflow, state, history, jobs, output,
revision, and last image were unchanged.

## Pass/fail summary and recommendations

- T1 compatibility generation: pass, with the separately documented initial
  command-runner supervision anomaly.
- T2 complete scene break and real image inspection: pass.
- T3 same-scene continue and real image inspection: pass.
- T4 failed-render rollback: pass.
- Telegram isolation: pass.
- Worker isolation: pass.
- Production immutability: pass.

An isolated production-state migration rehearsal on another read-only-derived
copy is recommended. Direct production deployment is not yet recommended merely
from this test: first review the retained T1 supervision evidence and accept the
authoritative workflow's persistent POV-style composition. No production
deployment or production state migration was performed.

