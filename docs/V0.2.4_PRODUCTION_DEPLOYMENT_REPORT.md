# v0.2.4 Production Deployment Report

Deployment date: 2026-07-28 (Australia/Sydney)

## Result

Production deployment succeeded without rollback. Production is running
`yinyue-avatar` v0.2.4 with schema-v2 state at revision 197. The authoritative
production workflow, including its intentionally fixed first-person/POV prompt
semantics, was preserved.

## Release source and freeze

- Repository:
  `/mnt/data_nvme/projects/hermes-skills/roleplay/yinyue-avatar`
- Branch: `feature/v0.2.4-long-term-memory-scene-state`
- Staging report commit: `86d9e98690585ad4ef1c41701e3affa037fd3e64`
- Required ancestors verified: `a22b703`, `94feb48`, `e9769c9`, and `86d9e98`
- Formal release commit:
  `d4f98c6b614402b2a010520c365e5a238c72bcc3`
- Annotated tag: `v0.2.4`, resolving to the formal release commit
- The working tree was clean before deployment.
- Deployment source was the permanent Git worktree. Ignored test bytecode was
  removed before installation; no uncommitted `/tmp` content was deployed.
- `v0.2.3` was not modified.

The annotated tag records structured long-term memory, meaningful recent
history, author-level relationship overwrite/restore, the scene state machine,
the safe installer, successful real-ComfyUI staging T1-T4, and intentional
preservation of fixed production POV semantics.

## Release tests

The following passed before production installation and were repeated as
appropriate from the formal release commit/tag:

- Shell syntax validation
- Python syntax validation
- JSON and schema parsing
- 55 of 55 current unit tests
- 8 of 8 original v0.2.3 regression tests from `v0.2.3`
- Safe installer dry-run/path/local-config/rollback tests
- Isolated staging preparation and cleanup tests
- Sensitive-information scan
- Workflow raw SHA-256:
  `6e5c6b879168e31ceb9af04fd6ffa4d4412618398699e415681b7d45a43a5f3d`
- Workflow masked SHA-256:
  `279d35360af5a1d5ed402cf11855732237b199130aeba1741d5483ce6db17208`

No workflow file or POV prompt was changed by the release freeze.

## Production baseline and recovery

Production was idle before deployment:

- No queued or running jobs
- No `_worker`, detached avatar worker, or systemd avatar unit
- `avatar.lock` was not held
- Revision: 196
- Last image:
  `/home/james/.hermes/state/yinyue-avatar/output/yinyue-20260728-142449-r0196.png`
- History lines: 196
- Job files: 200 (189 completed, 11 historical failed)
- Output files: 196
- Pending files: 1

The one pending file was a pre-existing legacy Telegram failure from
2026-07-19:
`pending/8cc4b6b1-3e38-4fdc-97a2-b5af9a344252.json`. Its SHA-256 remained
`d4cf7f7d0657828c2d22fa0cfe219ffae181e12719b5be1a2881dc9e33c2b901`
through deployment. It was not an active job and no new pending file was
created.

Lightweight pre-deployment checkpoint:

`/mnt/data_nvme/backups/hermes/yinyue-avatar/pre-v0.2.4-production-20260728T173836+1000`

The checkpoint is mode `0700`; sensitive files are mode `0600`. It contains
the prior production Skill, `state.json`, `history.jsonl`, `jobs/`, `pending/`,
local config, manifests, counts, workflow hashes, partition hashes, revision,
and last-image metadata. Historical output images were intentionally not
duplicated.

The existing v0.2.3 full recovery archive was revalidated:

- Archive:
  `/mnt/data_nvme/backups/hermes/yinyue-avatar/v0.2.3-20260728-145922/yinyue-avatar-v0.2.3-full-recovery-20260728-145922.tar.zst`
- SHA-256:
  `fe1ce9a04a27d8eddabf73ce977a81000cc42d6e0ffe9ccc0599e28e55cde8ba`
- Restore script:
  `/mnt/data_nvme/backups/hermes/yinyue-avatar/v0.2.3-20260728-145922/restore-v0.2.3.sh`

The full restore dry-run passed and resolved the exact production Skill and
state targets without changing files. Exact fallback commands:

```bash
bash /mnt/data_nvme/backups/hermes/yinyue-avatar/v0.2.3-20260728-145922/restore-v0.2.3.sh \
  --mode full

bash /mnt/data_nvme/backups/hermes/yinyue-avatar/v0.2.3-20260728-145922/restore-v0.2.3.sh \
  --mode full --apply
```

## Safe installation

The installer dry-run used the exact supported command and production target.
It confirmed distinct source/destination, dry-run mode, disabled state
migration, and disabled worker/ComfyUI/Telegram actions. Source inspection
confirmed that apply preserves local config, creates a prior-Skill backup, uses
a staging directory, and validates staging before switching.

The supported `--apply` path completed successfully. Installer backup:

`/home/james/.hermes/backups/yinyue-avatar/install-20260728T073923Z`

It contains before/staging/after manifests, a prior-Skill archive, and the
previous Skill directory. Installation did not migrate state, start a worker,
submit a ComfyUI prompt, or send Telegram.

Post-install checks:

- `SKILL.md` and `config.json` report `0.2.4`.
- `config.local.json` was preserved byte-for-byte, SHA-256
  `aa18f73adb0d8a1045b04f88f3d40cadd8c520da0f99e728a21f77481f864efc`.
- Production ComfyUI URL remained `http://192.168.100.211:8188`.
- Telegram channel configuration remained `telegram`.
- Production workflow raw and masked hashes matched the accepted baseline.
- The fixed `第一人称视角,POV` prompt remains present.
- Except for preserved local config, installed files matched the `v0.2.4` tag.

## State migration

Code inspection established that `context` calls `ensure_runtime`, which
creates required runtime directories, migrates schema 1 to 2 under the avatar
lock, writes a pre-migration backup, appends one migration event, and performs
legacy-memory bootstrap only when legacy memory contains importable content.

The first sandboxed attempt was blocked before any production write by the
execution environment's read-only filesystem. State remained schema 1,
revision 196, with 196 history lines and no migration backup/event. The
authorized production `context` invocation then performed the migration once.
This was an execution-environment permission retry, not a repeated migration.

Migration result:

- Schema: 1 to 2
- Revision: 196 to 196, as designed
- History: 196 to 197
- Migration event:
  `{"at":"2026-07-28T07:40:56+00:00","event":"migration","from_schema":1,"to_schema":2,"revision":196,"backup":"/home/james/.hermes/state/yinyue-avatar/baseline-backups/state-v0.2.3-pre-migration-r0196.json"}`
- Legacy-memory import events: 0 (the legacy recent-memory list was empty)
- Migration backup:
  `/home/james/.hermes/state/yinyue-avatar/baseline-backups/state-v0.2.3-pre-migration-r0196.json`
- Memory summary:
  `/home/james/.hermes/state/yinyue-avatar/memory/summary.json`
- Memory events are designated for
  `/home/james/.hermes/state/yinyue-avatar/memory/events.jsonl`; no file was
  created because there were no events to import.
- Memory summary backup directory:
  `/home/james/.hermes/state/yinyue-avatar/memory/backups`

The legacy `character`, `visual`, `internal`, `continuity`, and recent-memory
views compared equal after accounting only for the documented new continuity
metadata. The old relationship values were preserved in the new relationship
object, including stage 2, trust 20, intimacy/attachment 20, and legacy
compatibility fields. The prior last image remained present.

A second loader invocation was an idempotent no-op:

- Revision remained 196.
- State bytes and history line count did not change.
- Migration event count remained 1.
- Legacy-memory import event count remained 0.
- Relationship and last image did not change.

## Read-only and local validation

Production `--help`, `context`, `status`, `doctor`, `memory-list`, and `jobs`
completed without traceback and returned valid output. `relationship-status`
is not an implemented v0.2.4 subcommand (parser exit 2); the relationship
object was therefore validated through the implemented `status` output.

`context` loaded:

- Persona
- Lore summary
- Relationship
- Scene and emotion
- Memory summary
- Eight meaningful recent-history events

`doctor` returned `ok=true`. Python, workflow, state, state-directory
writability, Hermes CLI, systemd-run discovery, and the ComfyUI
`/system_stats` check all passed. No workflow mismatch, worker start, ComfyUI
submission, or Telegram send occurred during these checks.

## Real production generation

Command:

```bash
timeout --signal=TERM --kill-after=30s 420s \
  /home/james/.hermes/skills/roleplay/yinyue-avatar/bin/avatarctl \
  show --foreground --no-send
```

The production config had a 900-second ComfyUI generation timeout and a
2-second poll interval.

Result:

- Start: `2026-07-28T17:42:55+10:00`
- End: `2026-07-28T17:45:08+10:00`
- Elapsed: 132.95 seconds
- Job ID: `a0133a2102bc4bc59a801bf5ccdd190c`
- Prompt ID: `5cb97b87-0079-4e22-82be-5145d4824b80`
- Job status: completed
- Revision: 196 to 197
- History: 197 to 198 lines
- Image:
  `/home/james/.hermes/state/yinyue-avatar/output/yinyue-20260728-174508-r0197.png`
- Image: valid RGB PNG, 1632 by 2176, 3,978,258 bytes
- Delivery: `ok=true`, `attempted=false`, `skipped=true`
- No workflow mismatch, detached worker, or systemd avatar worker

The fixed POV style was intentionally accepted and was not treated as a
failure.

## Telegram delivery

The generated image was not regenerated. Production ran:

```bash
/home/james/.hermes/skills/roleplay/yinyue-avatar/bin/avatarctl \
  resend-last --foreground --say '银月 v0.2.4 生产发布验证'
```

Result:

- Start: `2026-07-28T17:45:43+10:00`
- End: `2026-07-28T17:45:55+10:00`
- Job ID: `70611454d797471bbfde315ec99112d2`
- Job status: completed
- Channel: current configured Telegram channel
- Text delivery: succeeded on attempt 1
- MEDIA delivery: succeeded separately on attempt 1
- Image sent:
  `/home/james/.hermes/state/yinyue-avatar/output/yinyue-20260728-174508-r0197.png`
- State bytes unchanged
- Revision remained 197
- History remained 198 lines
- Last prompt ID remained `5cb97b87-0079-4e22-82be-5145d4824b80`
- No new ComfyUI prompt
- Pending count remained 1; no new error pending

## Final production state

- Production version: 0.2.4
- State schema: 2
- Revision: 197
- Last image:
  `/home/james/.hermes/state/yinyue-avatar/output/yinyue-20260728-174508-r0197.png`
- History lines: 198
- Job files: 202
- Output files: 197
- Active queued/running jobs: 0
- Latest generation and resend jobs: completed
- Detached/systemd avatar workers: none
- `avatar.lock`: free
- Rollback performed: no

## Non-blocking observations

- The single explained 2026-07-19 legacy pending-delivery file remains
  unchanged. It predates v0.2.4 and is not associated with this deployment.
- `relationship-status` is mentioned in the deployment checklist but is not an
  actual v0.2.4 CLI subcommand; `status` provides the relationship state used
  for validation.
- The first migration command attempted inside a read-only execution sandbox
  was blocked before writes. The authorized production invocation then
  migrated exactly once.
